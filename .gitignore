# Base de trabajo: son los datos reales del negocio, no van al repositorio.
# La base de arranque que sí se versiona es datos/inicial.json
datos/base.json
datos/base.json.tmp

# Credenciales del proyecto de Firebase de cada cliente.
# No son secretas (viajan en el cliente), pero cada instalación tiene la suya
# y no corresponde versionar la de uno en la base común.
datos/firebase.json

# Respaldos descargados
base-*.json

node_modules/
dist/
.DS_Store
Thumbs.db
*.log
